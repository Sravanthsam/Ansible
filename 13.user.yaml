- name: user creation
  hosts: local
  connection: local
  tasks: 
  - name: verify user
    ansible.builtin.command: id expense
    register: USER

  - name: print user info
    ansible.builtin.debug:
      msg: "user info {{USER}}"
